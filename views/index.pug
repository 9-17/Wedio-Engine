doctype html
html
    head
        include lib/headSetting.pug

        link(rel="stylesheet", href="/css/index.css")
            
        title Wedio Engine
    body
        include lib/nav.pug
        
        .box
            .inbox
                h1 Wedio Engine
                h4 겁나 쩌는 엔진 입니다;; ㄷㄷ 빨리 쓰셈..
                -if(!username)
                    .loginBtn#googleLogin
                        a(href='/auth/google')
                            img.loginLogo#googleLogo(src="/img/g-logo.png", alt="구글 아이콘", align="top")
                            span.loginText#googleText Google로 로그인
                    br
                    .loginBtn#naverLogin
                        a(href='/auth/naver')
                            img.loginLogo#naverLogo(src="/img/n-logo.png", alt="네이버 아이콘", align="top")
                            span.loginText#naverText Naver로 로그인
                - else
                    button.btn.goCloud(onclick="location.href='/cloud'") 시작하기
                    

        include lib/footer.pug