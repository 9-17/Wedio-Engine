ul.navUl
    li.navLi.title
        a(href="/cloud") 
            img(src="/img/w-logo2.png", alt="wedio 로고")
    - if(user) 
        li.navLi.logout
            a(href="/auth/logout") 로그아웃
        li.navLi.username
            a(href="#") #{user.name}
        li.navLi.userPhoto
            a(href="#")
                img(src=user.photo, alt="프로필 사진")