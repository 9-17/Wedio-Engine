ul.navUl
    li.navLi.title
        a(href="#") WedioLOGO
    - if(username) 
        li.navLi.logout
            a(href="/auth/logout") 로그아웃
        li.navLi.username
            a(href="#") #{username}